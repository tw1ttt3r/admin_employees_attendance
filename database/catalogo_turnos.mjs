import { METADATA } from "#db/metadata.mjs";

const CATALOGOTURNOS = {
  IDTURNO: 'id_turno',
  DESCRIPCION: 'descripcion',
  SIGLA: 'sigla',
  ESTATUS: 'estatus',
  ...METADATA
};

export {
  CATALOGOTURNOS
};